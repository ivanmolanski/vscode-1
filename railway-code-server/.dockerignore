# Railway code-server deployment — local deploy context
# Deploy with:  railway link -s code-server && railway up
src/
*.log
node_modules/
.git/
dist/
out/
**/.vscode-oss-dev/
# local eslint scaffolding (never bake into the build)
eslint.config.js
.eslintrc*
# never bake credentials into the image
.railway-password.txt
.railway-sudo-password.txt
.railway/
